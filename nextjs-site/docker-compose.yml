services:
  nextjs-site:
    build: .
    image: nextjs-site:latest
    container_name: nextjs-site
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    command: npm start
